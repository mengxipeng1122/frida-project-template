{
  "name": "criuo-n-blast",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "web",
    "frida"
  ],
  "scripts": {
    "build_frida": "npm run build -w frida",
    "build": "npm run build -w web",
    "dev": "npm run dev -w web",
    "test": "npm run test -w web"
  },
  "devDependencies": {
    "ts-node": "^10.9.2",
    "typescript": "^5.0.0"
  },
  "dependencies": {
    "canvas": "^2.11.2",
    "gl": "^8.1.6"
  }
}
